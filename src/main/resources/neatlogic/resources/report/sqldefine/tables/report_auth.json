{
  "moduleId": "report",
  "name": "report_auth",
  "label": "报表模版授权表",
  "description": "报表模版授权表 可用于报表相关的关联查询等报表查询场景。",
  "fields": [
    {
      "name": "report_id",
      "type": "bigint",
      "description": "报表模版ID，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "type",
      "type": "enum('user','role','team','common')",
      "description": "common：公共，user：用户,role：角色,team：分组，作为当前表记录的主键或唯一定位字段。"
    },
    {
      "name": "auth_uuid",
      "type": "char(32)",
      "description": "uuid，作为当前表记录的主键或唯一定位字段。"
    }
  ],
  "indexes": [
    {
      "name": "PRIMARY",
      "type": "BTREE",
      "unique": true,
      "primary": true,
      "columns": [
        "report_id",
        "type",
        "auth_uuid"
      ],
      "description": "主键索引，覆盖 report_id、type、auth_uuid 字段，适合用于唯一定位记录和主表关联查询。"
    },
    {
      "name": "idx_uuid",
      "type": "BTREE",
      "unique": false,
      "primary": false,
      "columns": [
        "auth_uuid"
      ],
      "description": "普通索引，覆盖 auth_uuid 字段，适合用于过滤、关联或排序查询。"
    }
  ]
}
